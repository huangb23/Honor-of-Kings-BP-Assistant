# backend — BP 胜率预测与选人推荐器

给定"我方部分/满阵容 vs 敌方部分/满阵容 + 已 ban 英雄"，预测胜率，并推荐：
1. **单英雄选人**：按分路给出各位置的 Top8 推荐英雄及对应胜率
2. **双英雄选人**：给出最优的 2 英雄组合及对应胜率

## 快速使用

```bash
cd backend
python3 -u main.py \
    --my 百里守约,墨子,元流之子(坦克) \
    --opp 少司缘,元歌,西施,孙尚香,云缨 \
    --ban 瑶
```

参数：
| 参数 | 说明 |
|------|------|
| `--my` | 我方已选英雄，逗号分隔（可空/部分）|
| `--opp` | 敌方已选英雄，逗号分隔（可空/部分）|
| `--ban` | 已 ban 英雄，逗号分隔（可选）|
| `--role` | 只推荐某分路（对抗路/中路/发育路/打野/游走），默认全部分路 |
| `--mode` | 胜率分段：`dianfeng`=巅峰千强，`dazhong`=大众分段（默认）|
| `--force` | 强制重新爬取组合优势（默认按过期自动判断）|

**规则**：已选（无论我方还是敌方）与已 ban 的英雄不会再出现在推荐里。

**未满 5v5 处理**：英雄视角按 5 个槽位归一，未选槽位按「中性 50 胜率、无协同/克制」计分（csv=50，syn=cnt=0，归克制数 0 组）；查不到胜率的真实英雄按 45 兜底。阵营得分恒 /5，因此部分阵容与满阵容可比。满 5 人后即为校准的绝对胜率。

---

## 数据更新

### 英雄胜率（多分段）
来源：`https://tianyuanzhiyi.com/api/herostats?date=YYYY-MM-DD&gameMode={1|3|4|6}`
- 分段：`1`=全分段，`3`=1350+，`4`=顶端排位，`6`=巅峰千强（近5日聚合）。
- 爬虫抓取全部 4 个分段，每英雄保存 `m1/m3/m4/m6` 胜率；某分段缺失用 45 兜底。
- 展示分段：
  - `dianfeng`（巅峰千强）：直接用 `m6`。
  - `dazhong`（大众分段）：每个英雄的 csv 胜率按它的「对手克制数 k（0..5）」在 大众↔巅峰 间插值：
    `csv = 大众聚合值 + (巅峰千强 − 大众聚合值) × k/5`；其中大众聚合值 = `(m1*0.5 + m3 + m4 + m6*0.5) / 3`。
    即：克制数 0 → 大众胜率，克制数 5 → 巅峰千强胜率，中间线性过渡。
- 每天只更新前一天数据，优先用昨天；当天接口若已就绪则用当天。
- **同时抓取前一天数据**（用于日环比变动榜）：最新日确定后继续向前回溯抓前一天；抓不到则只保存最新一天。
- **每天自动刷新**：若 `hero_winrate.json` 的 `fetched_at` 不是今天、或缺少前一天数据（`heroes_prev`），则重新拉取；当天已爬过则复用。
- 保存到 `data/hero_winrate.json`（最新一天存 `heroes`，前一天存 `heroes_prev`，日期存 `dates`）。

### 组合优势（协同/克制指数）
来源：`https://tianyuanzhiyi.com/api/hero/analysis?heroId={id}`
- **英雄名册**直接从站点 `herostats` 接口拉取全量（含新英雄），保存为 `data/hero_registry.json`；站点失败才回退本地缓存 / 旧 1.json。
- 抓取每个英雄的 `goodSynergies`/`badSynergies`（协同）和 `counters`/`counteredBy`（克制），**并保留每条关系的 `totalMatches`（频次）**。
- 汇总为一份 `data/combo_advantage.json`（与胜率英雄数一致，当前 132）；每条关系形如 `{"idx": 指数, "m": totalMatches}`。
- **过期规则**：该文件距今 >= 5 天则重新爬取；否则直接复用。

---

## 数据文件（data/）

| 文件 | 内容 |
|------|------|
| `hero_winrate.json` | 每英雄多分段胜率（m1/m3/m4/m6），含最新一天 `heroes` 与前一天 `heroes_prev` |
| `combo_advantage.json` | 每英雄的协同/克制指数（含每条关系的 totalMatches）|
| `hero_registry.json` | 英雄名 → id 映射（缓存）|

---

## HTTP 服务（server.py）

| 接口 | 说明 |
|------|------|
| `GET /api/heroes` | 全部英雄与分路（供前端选人）|
| `GET /api/leaderboard?mode={dianfeng\|dazhong}&top=10` | 胜率榜 TopN + 日环比变动榜（上涨/下跌 TopN），按分段切换 |
| `POST /api/predict` | BP 预测；`single_pick` 每项为 `(hero, 加入后胜率, 自身胜率)`，`double_pick` 每项为 `((a,b), 组合胜率, (a自身, b自身))`，自身胜率按请求分段返回 |

---

## 模型（英雄视角 24 权重：z=3.0 截断 + 强/弱克制数分组）

阵营得分拆到英雄视角：每个英雄按「对手中强/弱克制关系」落在 8 个克制组之一，
用该组的权重 `(csv/syn/cnt)` 乘它的三项评分，阵营得分 = 5 英雄评分之和 / 5，双方相减得 logit。

```
关系值预处理（z=3.0 置信截断，抑制低场次大指数的噪声夸大）：
  idx_eff = sign(idx)·min(|idx|, μ_post + 3σ_post)
  μ_post = |idx|·τ²/(τ²+s²)，σ_post = sqrt(1/(1/τ²+1/s²))，s = 50/√m，τ = 1.83pp

每个英雄评分（按强/弱克制情况选权重组）：
  对 5 个对手中每条有效关系（totalMatches >= 50）的截断值 c：
    强克制：|c| >= 4.0；弱克制：有效且 |c| < 4.0
  组索引 k = min(强克制数,3)×2 + (有弱克制 ? 1 : 0)   → 8 组
  hero_score = w_csv[k]·csv_i + w_syn[k]·syn_i + w_cnt[k]·cnt_i
    csv_i = 该英雄站点 csv 胜率（百分数；大众分段按克制强度 kf=强数+0.5×弱数 插值）
    syn_i = (对队友有效协同截断值之和) / 有效协同条数   # 0 条则 0
    cnt_i = (对对手有效克制截断值之和) / 有效克制条数   # 0 条则 0

score = (Σ 我方 hero_score)/5 − (Σ 敌方 hero_score)/5
win_rate = 1 / (1 + e^{−score})
```

24 个权重（`model.W_CNT`，8 组 × csv/syn/cnt）：

| 组（强数,弱有无） | csv | syn | cnt |
|--------|-------|-------|-------|
| 0,无 | -0.0413 | +0.1150 | 0 |
| 0,有 | -0.0396 | +0.0782 | +0.1154 |
| 1,无 | -0.0330 | +0.0976 | +0.0513 |
| 1,有 | -0.0374 | +0.0917 | +0.1498 |
| 2,无 | -0.0289 | +0.2540 | +0.1819 |
| 2,有 | -0.0363 | +0.0518 | +0.2296 |
| 3+,无 | -0.0188 | +0.1544 | +0.4469 |
| 3+,有 | -0.0150 | +0.1954 | +0.2544 |

- 无独热、无截距、无需归一化
- 需要 combo 数据含 `totalMatches`（`model.MIN_M=50` 过滤）
- 实验过程与选型依据见 `training/SUMMARY.md` 附录（CV acc 0.5737 vs 旧 18 权重 0.5728，差异在噪声内；
  选型动机：强/弱分组消除微弱克制导致的组间跳变，z=3.0 截断封顶低场次野值）

---

## 模块

| 文件 | 作用 |
|------|------|
| `main.py` | 命令行入口 |
| `server.py` | BP 预测 HTTP 服务（含 /api/leaderboard 排行榜接口）|
| `winrate_crawler.py` | 胜率爬虫（最新一天 + 前一天）|
| `combo_crawler.py` | 组合优势爬虫（含过期检查，保存 totalMatches）|
| `model.py` | 英雄视角 24 权重模型（z=3.0 截断 + 强/弱克制数分组）|
| `engine.py` | BP 推荐引擎（单英雄 Top8 / 双英雄组合）|
| `config.py` | 全局配置（站点、天数等）|